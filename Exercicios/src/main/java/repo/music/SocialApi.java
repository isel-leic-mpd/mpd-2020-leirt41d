package repo.music;

import java.util.concurrent.CompletableFuture;

public class SocialApi {
    // retorna o identificador (mbid) do cantor favorito do utilizador userId.
    pubic static CompletableFuture<String> getFavSingerAsync(int userId) {
        return null;
    }
}
