package repo.movies.reactive;

import io.reactivex.Observable;

import java.util.concurrent.CompletableFuture;

public class MoviesApi {
    // asynchronously get the rating for the movie with the title passed
    public static Observable<Double>
    getRating( String title) { return null; }
}
